@model int

<span class="badge rounded-pill @(Model > 0 ? "bg-cyber-neon text-dark fw-bold" : "bg-secondary text-light") ms-1 cyber-compare-badge font-monospace" id="compare-counter-badge">
    @Model
</span>
